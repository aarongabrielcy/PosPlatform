<Window x:Class="Pos.Desktop.Enforcement.SuspensionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Instalación suspendida — PosPlatform"
        Height="320" Width="420"
        MinHeight="320" MinWidth="380"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">
    <Grid Margin="32">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                    Text="PosPlatform"
                    FontSize="22" FontWeight="Bold" Margin="0,0,0,16" />

        <TextBlock Grid.Row="1"
                    Text="{Binding Message}"
                    TextWrapping="Wrap" VerticalAlignment="Top" />

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button x:Name="CerrarCajaAbiertaButton" Content="Cerrar caja abierta" Width="160" Margin="0,0,8,0"
                    TabIndex="0" Click="OnCerrarCajaAbiertaClick" />
            <Button x:Name="SalirButton" Content="Salir" Width="100"
                    TabIndex="1" IsCancel="True" IsDefault="True" />
        </StackPanel>
    </Grid>
</Window>
