<Window x:Class="Pos.Desktop.Sales.Checkout.CheckoutWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Cobrar — PosPlatform"
        Height="480" Width="440"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter" />
    </Window.Resources>
    <Grid Margin="32" IsEnabled="{Binding IsNotBusy}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                    Text="Cobrar"
                    FontSize="20" FontWeight="Bold" Margin="0,0,0,16" />

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="160" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <Label Grid.Row="0" Grid.Column="0" Content="Total" />
            <TextBlock Grid.Row="0" Grid.Column="1" Margin="0,10" VerticalAlignment="Center"
                    FontSize="16" FontWeight="Bold" Text="{Binding TotalAmountText, Mode=OneWay}" />

            <Label Grid.Row="1" Grid.Column="0" Content="Forma de pago" />
            <StackPanel Grid.Row="1" Grid.Column="1" Orientation="Horizontal" Margin="0,10">
                <RadioButton x:Name="CashRadioButton" Content="Efectivo"
                        GroupName="PaymentMethod" Margin="0,0,16,0"
                        IsChecked="{Binding IsCashSelected}" />
                <RadioButton x:Name="CardRadioButton" Content="Tarjeta — manual"
                        GroupName="PaymentMethod"
                        ToolTip="Cobro realizado en una terminal externa; PosPlatform solo registra la referencia/autorización."
                        IsChecked="{Binding IsCardSelected}" />
            </StackPanel>

            <StackPanel Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2"
                    Visibility="{Binding IsCashSelected, Converter={StaticResource BoolToVisibilityConverter}}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="160" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <Label Grid.Row="0" Grid.Column="0" Content="Efectivo recibido"
                            Target="{Binding ElementName=CashTenderedTextBox}" />
                    <TextBox x:Name="CashTenderedTextBox" Grid.Row="0" Grid.Column="1"
                            Margin="0,6" Padding="6,4" TabIndex="0"
                            Text="{Binding CashTenderedText, UpdateSourceTrigger=PropertyChanged}" />

                    <Label Grid.Row="1" Grid.Column="0" Content="Cambio" />
                    <TextBlock Grid.Row="1" Grid.Column="1" Margin="0,10" VerticalAlignment="Center"
                            FontWeight="Bold" Text="{Binding ChangeText, Mode=OneWay}" />
                </Grid>
            </StackPanel>

            <StackPanel Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="2"
                    Visibility="{Binding IsCardSelected, Converter={StaticResource BoolToVisibilityConverter}}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="160" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <Label Grid.Row="0" Grid.Column="0" Content="Referencia / autorización"
                            Target="{Binding ElementName=CardReferenceTextBox}" />
                    <TextBox x:Name="CardReferenceTextBox" Grid.Row="0" Grid.Column="1"
                            Margin="0,6" Padding="6,4" TabIndex="0"
                            Text="{Binding CardReferenceText, UpdateSourceTrigger=PropertyChanged}" />
                </Grid>
            </StackPanel>
        </Grid>

        <TextBlock Grid.Row="2" Text="{Binding GeneralError}"
                    Foreground="Red" TextWrapping="Wrap" Margin="0,8,0,0" />

        <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,8,0,0"
                    Visibility="{Binding IsBusy, Converter={StaticResource BoolToVisibilityConverter}}">
            <ProgressBar IsIndeterminate="True" Width="160" Height="6" VerticalAlignment="Center" />
            <TextBlock Text="Procesando cobro…" Margin="8,0,0,0" VerticalAlignment="Center" />
        </StackPanel>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="Cancelar" Width="120" Margin="0,0,8,0" TabIndex="2"
                    Command="{Binding CancelCommand}" />
            <Button Content="Confirmar cobro" Width="140" TabIndex="1"
                    IsDefault="True" Command="{Binding ConfirmCommand}" />
        </StackPanel>
    </Grid>
</Window>
