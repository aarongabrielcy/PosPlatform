<Window x:Class="Pos.Desktop.RegisterSessions.OpenRegisterSessionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Apertura de caja — PosPlatform"
        Height="420" Width="480"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter" />
    </Window.Resources>
    <Grid Margin="32" IsEnabled="{Binding IsNotBusy}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                    Text="Apertura de caja"
                    FontSize="20" FontWeight="Bold" Margin="0,0,0,4" />
        <TextBlock Grid.Row="1"
                    Text="Indique el fondo inicial para comenzar a operar esta caja."
                    Margin="0,0,0,16" TextWrapping="Wrap" />

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="160" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <Label Grid.Row="0" Grid.Column="0" Content="Usuario" />
            <TextBlock Grid.Row="0" Grid.Column="1" Margin="0,10" VerticalAlignment="Center"
                        Text="{Binding UserDisplayName}" FontWeight="Bold" />

            <Label Grid.Row="1" Grid.Column="0" Content="Caja"
                    Target="{Binding ElementName=RegisterComboBox}" />
            <ComboBox x:Name="RegisterComboBox" Grid.Row="1" Grid.Column="1"
                    Margin="0,6" Padding="6,4" TabIndex="0"
                    Visibility="{Binding RegisterSelectorVisibility}"
                    ItemsSource="{Binding AvailableRegisters}"
                    SelectedItem="{Binding SelectedRegister}"
                    DisplayMemberPath="Name" />
            <TextBlock Grid.Row="1" Grid.Column="1" Margin="0,10" VerticalAlignment="Center"
                        Visibility="{Binding RegisterReadOnlyVisibility}"
                        Text="{Binding ReadOnlyRegisterName}" FontWeight="Bold" />

            <Label Grid.Row="2" Grid.Column="0" Content="Fondo inicial"
                    Target="{Binding ElementName=OpeningAmountTextBox}" />
            <TextBox x:Name="OpeningAmountTextBox" Grid.Row="2" Grid.Column="1"
                    Margin="0,6" Padding="6,4" TabIndex="1"
                    Text="{Binding OpeningAmountText, UpdateSourceTrigger=PropertyChanged}" />

            <Label Grid.Row="3" Grid.Column="0" Content="Moneda" />
            <TextBlock Grid.Row="3" Grid.Column="1" Margin="0,10" VerticalAlignment="Center" Text="MXN" />
        </Grid>

        <TextBlock Grid.Row="3" Text="{Binding GeneralError}"
                    Foreground="Red" TextWrapping="Wrap" Margin="0,12,0,0" />

        <StackPanel Grid.Row="4" Orientation="Horizontal" Margin="0,8,0,0"
                    Visibility="{Binding IsBusy, Converter={StaticResource BoolToVisibilityConverter}}">
            <ProgressBar IsIndeterminate="True" Width="160" Height="6" VerticalAlignment="Center" />
            <TextBlock Text="Abriendo caja…" Margin="8,0,0,0" VerticalAlignment="Center" />
        </StackPanel>

        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="Cerrar sesión" Width="120" Margin="0,0,8,0" TabIndex="3"
                    Command="{Binding LogoutCommand}" />
            <Button Content="Abrir caja" Width="120" TabIndex="2"
                    IsDefault="True" Command="{Binding OpenCommand}" />
        </StackPanel>
    </Grid>
</Window>
